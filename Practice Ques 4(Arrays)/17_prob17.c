/*
Given a sorted array with exactly one element appearing an odd number of times (all others
appear exactly twice), find that element using binary search in O(log n).
Hint: Use parity of the index: if index is even, arr[i]==arr[i+1] means the odd element is to the right
*/