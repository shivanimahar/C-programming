/* 4. Write a program to calculate simple interest for a set of values representing
principal, number of years and rate of interest. */
#include<stdio.h>

int main(){
    int t=6, r=7;
    float p = 3.24;
    
    printf("SI will be %d",(p*r*t)/100);
    return 0;
}