/*. [Array via Pointer to Function]
Write a C function void bubbleSort(int *arr, int n) that sorts an integer array using bubble sort,
accessing all elements exclusively via pointer arithmetic (no [] notation). Print the array before and
after sorting from main().
■ Hint: Use *(arr+i) and *(arr+j) for comparisons and swaps.
■ Tests: pointer + array, sorting, pass array as pointer.*/


