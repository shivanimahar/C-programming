// CQ16. Write a C program to demonstrate all compound assignment operators:
//       +=, -=, *=, /=, %=
//       Use a single variable and show the result after each operation.

#include<stdio.h>

int main(){
    int a=5;
    a+=10;
    printf("%d",a);


    return 0;
}













