/*
30. Write a C program that combines ALL four storage classes (auto, static,
    extern, register) in a meaningful way. Include:
    - An extern global variable for total count
    - A function with a static counter
    - A loop using register variable
    - A local auto variable for temporary computation
    Print descriptive output showing what each storage class contributes.
*/
