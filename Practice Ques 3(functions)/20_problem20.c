/*
19. Write a recursive C function to find the GCD (Greatest Common Divisor)
    of two numbers using the Euclidean algorithm.

    Formula: gcd(a, b) = gcd(b, a%b), base: gcd(a, 0) = a

    Test: gcd(48, 18) = 6
*/